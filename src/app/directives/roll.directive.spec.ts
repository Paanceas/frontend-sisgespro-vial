import { RollDirective } from './roll.directive';

describe('RollDirective', () => {
  it('should create an instance', () => {
    const directive = new RollDirective();
    expect(directive).toBeTruthy();
  });
});
